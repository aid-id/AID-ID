@model IEnumerable<aid_id.Models.Analisis>

@{
    ViewBag.Title = "Average";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Mis Informes</h2>
<div class="container">
    <div class="row">
        <div class="col-2">
<div class="dropdown">
    <a class="btn btn-primary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Promedio
    </a>

    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        <a id="pro-seven-id" class="dropdown-item" href="#">7 Días</a>
        <a id="pro-four-id" class="dropdown-item" href="#">14 Días</a>
        <a id="pro-three-id" class="dropdown-item" href="#">30 Días</a>
        <a id="pro-six-id" class="dropdown-item" href="#">60 Días</a>
        <a id="pro-nine-id" class="dropdown-item" href="#">90 Días</a>
</div>
    </div>
        </div>
        <div class="col-3">
        <div class="alert alert-primary" role="alert">
            <a href="#" class="alert-link">5</a>
        </div>

        </div>

        </div>
</div>


<div class="table-responsive div-food-class">
    <table class="table  table-striped">
        <thead class="table-dark">
            <tr>
                <th colspan="2">
                    Fecha
                </th>
                <th>
                    Comida
                </th>
                <th>
                    CH
                </th>
                <th>
                    Análisis
                </th>
            </tr>
        </thead>
        <tbody class="table-light">
            @foreach (var item in Model)
            {
                if (item.Id_usuario != 3) { continue; }
                <tr>
                    <td scope="col">
                        @Html.DisplayFor(modelItem => item.Fecha_hora)
                      
                    <td scope="col">
                    </td>
                    <td scope="col">
                        @Html.DisplayFor(modelItem => item.Tipocomida)
                    </td>
                    <td scope="col">
                        @Html.DisplayFor(modelItem => item.Carbo_totales)
                    </td>
                    <td scope="col">
                        @Html.DisplayFor(modelItem => item.Valor)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
 <script>
     $("#pro-seven-id").click(function () {
         //Calculo necesario
     

     });
     $("#pro-four-id").click(function () {
         //Calculo necesario
         
     });
     $("#pro-three-id").click(function () {
         //Calculo necesario
         
     });
     $("#pro-six-id").click(function () {
         //Calculo necesario
         
     });
     $("#pro-nine-id").click(function () {
         //Calculo necesario
         
     });

 </script>