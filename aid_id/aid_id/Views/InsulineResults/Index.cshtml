@model IEnumerable<aid_id.Models.Analisis>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Usuarios.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_hora)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Intensidad_deporte)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Usuarios.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_hora)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Intensidad_deporte)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_analisis }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_analisis }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_analisis })
        </td>
    </tr>
}

</table>
